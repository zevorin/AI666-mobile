# 赛事信息架构与去框体设计 QA

日期：2026-08-13

## 对比输入

- 参考：`tmp/tapnow-ia-audit-20260812/01-activity-description.png`
- 实现：`tmp/tapnow-ia-audit-20260812/05-ai666-final-ia.png`
- 并排复核：`tmp/tapnow-ia-audit-20260812/06-tapnow-vs-ai666-final-ia.png`

## 本轮目标

参考 TapNow 对复杂赛事信息的归类方式，但不复刻其视觉。AI666 继续沿用已确认的深色社区体系、电影感视频 Hero、暖金主强调和现有登录态。

## 可见结果

- [x] 赛事一级导航仅保留“活动介绍 / 参赛规则 / 参赛作品”，不再把赛程、奖项、评委等内容全部铺在导航栏。
- [x] C 端头部收敛为单层紧凑结构：平台生态入口当前全部隐藏；社区、漫剧创作、API 中转与赛事社群继续由后台可信配置保留，不生成第二层导航或空白占位。
- [x] 活动介绍以连续 Markdown 正文承载赛事简介；页面展示四阶段赛事进程、赛道赛题和三项竞技奖励，赛制只在参赛规则页呈现，不再公开评委阵容。
- [x] 参赛规则页只保留规则标题、目录和正文；正文承载参与方式与赛道、资格、投稿要求、评审维度、奖项与权益、版权与授权、违规处理与申诉。
- [x] 规则页不展示当前阶段发布上下文、完整赛事日程、规则版本、生效时间、Hero 说明文案、底部版本说明或“提交作品”按钮。
- [x] “奖项与权益”是独立一级章节，与“评审维度”平级。
- [x] 参赛作品页只展示标题、用户状态决定的投稿主动作、“全部作品 / 排行榜”内部切换、搜索/赛道/排序/全部与我的作品筛选和作品流；作品卡继续使用社区消费型卡片语言。
- [x] 参赛作品页不展示当前阶段公开栏、Hero 阶段时间说明或页尾紧凑赛程，避免在内容消费页重复赛事上下文。
- [x] 排行榜只展示榜单、我的名次、结果公示与领奖信息，页尾不重复展示完整赛事日程。
- [x] 首页 Hero 的主动作根据登录、报名、阶段、投稿状态和额度动态决定；状态标签只表达“已报名”等身份事实。
- [x] 首页没有再放精选作品、指南卡或规则卡，避免与独立页面重复。

## 去框体视觉复核

- [x] 七个赛事页面统一采用“内容优先于容器”：活动说明、赛事状态、赛程、规则、排名和侧栏信息以字号、留白、节点、左强调线或上下分隔组织，不再使用等权卡面。
- [x] 首页四阶段采用无连续连接轴的日程分段；当前阶段保持唯一 `aria-current="step"`，已完成、当前与后续状态仍可辨识，第四阶段直接展示结果公布时间。
- [x] 首页不再展示“赛制”信息列；评委评分和其他赛制信息统一从参赛规则页查看。
- [x] 赛道由四张同构面板降为结构化条目；奖励区只保留标题与冠军、亚军、季军三项竞技奖励及独立有效参赛奖励，不展示标题副文案、评选规则入口或赛后安排。
- [x] 结果公示、申诉和领奖信息由排行榜结果态及参赛规则页承载；主办方、创作支持和赛事咨询降为页尾轻量署名。
- [x] 投稿页去除大表单外壳和卡套卡侧栏，作品详情侧栏、排行榜行、规则目录与公式区改为连续信息结构。
- [x] 规则页不再用阶段条、版本信息和底部行动区打断正文；目录与 8 个一级章节形成唯一阅读结构。
- [x] 作品 4:3 媒体卡、视频播放器、输入、上传区、按钮、焦点态和弹层继续保留必要边界，没有因“去框”损失交互识别。
- [x] B 端继续遵循 Ant Design 管理后台习惯，本轮不跟随 C 端执行去框体改造。

## C / B 联动

- [x] B 端保留 10 个运营任务 Tab，没有为了本轮 IA 再扩一级模块。
- [x] “基础与展示”可配置平台可信目标、赛事导航及活动介绍 Markdown，并支持预览发布效果。
- [x] “评委与评分”继续承载内部评委、任务、进度和评分规则，不再维护 C 端公开评委资料。
- [x] 公开规则由结构化事实源编排，不再依赖一段自由文本复制比赛事实。
- [x] B 端继续维护规则版本号、生效时间、发布范围、字段级差异、通知和审计记录；这些治理信息不在 C 端规则页显性展示。
- [x] B 端继续维护活动总时间、时区、阶段顺序、阶段起止时间、动作矩阵和进入条件；参赛作品页移除阶段展示不改变后台配置及其对投稿、互动、投票和指标的控制。
- [x] “投稿与作品”区分投稿规则、作品库和审核队列，避免把公开作品与待审版本混为一层。

## 浏览器与验证

- [x] C 端浏览器复验：赛事导航 3 项、可见平台入口 0 项、桌面头部约 64px 且 Hero 紧随其后；活动介绍为无背景无边框的连续正文；首页连续连接轴、赛制列、奖项副文案、评选规则入口、奖项赛后安排、公开评委阵容和旧摘要框均为 0，主办信息仅保留页尾轻量署名，横向溢出为 0。
- [x] B 端浏览器复验：10 个任务 Tab 保持不变；Markdown 编辑、实时预览与保存链路可用；公开评委配置为 0，后台评委管理仍保留，横向溢出为 0。
- [x] C 端 Public Preview 28/28 页面通过；赛事专项、产品合同与 C/B 覆盖通过。
- [x] 桌面端复核首页、规则、投稿、作品广场、排行榜与作品详情；规则页只保留规则标题、目录和正文，奖项与权益为独立一级章节；作品广场只保留标题、投稿主动作、内部切换、筛选和作品流，当前阶段公开栏、阶段时间说明及页尾紧凑赛程均为 0；390×844 首页横向溢出为 0，当前阶段语义唯一，控制台无新增错误。
- [x] B 端 31 个生成页完成重建；静态与关键交互验证通过。

## 边界

- 本轮没有实现“活动外发现、活动内投票”的跨页面分发能力；只保留后续接入位置。
- API 中转入口默认隐藏；开启前必须先配置为平台可信目标，赛事页面不接受任意外链。
- 这是本地产品原型与配置演示，不代表真实接口、上传和线上发布已完成。

final result: passed

## 移动社区“共创广场”方案一实现（2026-08-20）

### 对比输入

- 选定视觉源：`C:/Users/hongx/.codex/generated_images/01a01d3e-53d8-7160-bddd-5bcbdcb3c605/exec-1815db1d-eca6-4659-935c-cfc3bc34bd89.png`，853 × 1844。
- 实现截图：`tmp/community-hub-option1-design-qa-20260820/implementation-final-390x844.png`，390 × 844；使用 `mobile-community.html?clean=1` 排除仅供内部走查的辅助触发器。
- 同屏对照：`tmp/community-hub-option1-design-qa-20260820/comparison-final.png`；视觉源等比缩放为 390 × 844 后与实现并排比较。
- 首屏主视觉资产：`assets/image_assets/community-hub-featured-v1.png`，1506 × 1045；按“青蓝盘龙宫殿、凤冠神女、左侧深色留白、无界面文字”生成并用于真实页面裁切。

### 分级发现与修复

- P1：原移动社区只有 AIGC 与闪念两个并列频道，无法承载教程，也缺少跨内容类型的社区聚合首页；已实现推荐、AIGC、闪念、教程四频道，并将推荐设为默认聚合视图。
- P1：全局逻辑复核发现活动“立即参与”仍直接进入 AIGC，与桌面端“生成新作品 / 直接上传已有作品”双路径不一致；已增加参与方式选择、已有图片上传、标题与说明校验。
- P2：精选区需要与选定设计一致的左文右图叙事，而现有作品图无法提供左侧留白；已使用独立主视觉资产，并以渐变遮罩保证标题、说明和主动作的对比度。
- P2：教程若跳回桌面详情会破坏移动骨架；已改为移动社区内教程频道和底部预览层，不新增桌面路由依赖。

### 必要保真面

- 布局：保留“社区标题 → 四频道 → 今日精选 → 闪念热议 → 官方教程 → 五项底部导航”的顺序、深色满宽区块和暖金选中态。
- 字体与内容：精选标题使用宋体回退形成东方编辑感；其他文字继续沿用项目字体栈。示例作者、模型、讨论内容和教程元信息均为可读的真实样例。
- 图像与图标：精选、闪念缩略图、教程封面均使用真实位图；交互图标全部来自本地 Remix Icon，没有手写 SVG、CSS 图形或占位素材。
- 可访问性：频道使用 tab/tabpanel 与 aria-selected；Flash 和教程弹层均可关闭；关键主动作保持不小于 44px 的移动点击区域，因此局部垂直密度略高于静态视觉源但不改变信息层级。

### 交互与响应式复核

- [x] 推荐为默认频道；AIGC、闪念、教程、推荐四项逐一切换时始终只有一个面板可见，URL module 参数与 aria-selected 同步。
- [x] 推荐页“发闪念”打开既有发布层；教程卡打开移动预览层，标题和时长随卡片更新，两者均可关闭。
- [x] 生图活动参与方式层同时提供 AIGC 生成和直接上传已有作品；直传表单未选图片时显示明确校验，标题与说明沿用活动既有门槛。
- [x] 24 个移动页面 × 360 × 800、390 × 844、430 × 932 共 72 组真实浏览器检查，横向溢出、损坏资源、空页面、固定导航/动作条、意外打开弹层均为 0。
- [x] 移动端专项验证 24/24 通过，禁止文案匹配为空；最终同屏对照无剩余 P0/P1/P2 问题。

final result: passed

## 社区移动生成结果页沉浸式重构（2026-08-20）

### 对比输入

- 视觉事实：`C:/Users/hongx/.codex/generated_images/01a01d3e-53d8-7160-bddd-5bcbdcb3c605/exec-d4125f9b-219a-489b-818d-147400cf6c90.png`。
- 实现截图：`tmp/mobile-generation-result-design-qa-20260820/implementation-final-390x844.png`。
- 全视图对照：`tmp/mobile-generation-result-design-qa-20260820/comparison-final-390x844.png`。
- 重点区域对照：`tmp/mobile-generation-result-design-qa-20260820/comparison-focused-content-actions.png`。
- 视口与状态：实现页为 390 × 844 CSS 视口、生成成功、图片模式、创作信息收起、去向选择层关闭；853 × 1844 视觉稿按同一视口比例居中裁切并归一化为 390 × 844。

### 分级发现与修复历史

- P1：无。
- P2：首轮实现仍保留卡片边界、媒体占比不足，标题和底部动作层级偏重；已改为“轻状态行 → 全宽媒体 → 连续创作信息 → 固定动作栏”的单一阅读路径。
- P2：复用原图时凤冠和裙摆被 5:6 媒体框裁切；已基于用户确认稿与原作品图生成独立全身主视觉 `assets/image_assets/15-mobile-result-v2.png`，主图不含 UI、文字或水印。
- P2：第二轮对照中主图高度、说明密度和浏览器滚动条仍偏离确认稿；已收敛为 5:6 媒体框、四行说明、44px 展开入口，并隐藏结果页滚动条视觉。
- P3：系统字体渲染和生成主图局部纹理与视觉稿存在平台级差异，不影响层级、构图、操作识别或验收。

### 必要保真面

- 字体与层级：顶部只保留任务标题；成功状态降为一行轻提示；作品标题使用当前深色社区允许的衬线展示字，模型与比例用暖金强调，正文保持可读但不抢主图。
- 间距与节奏：媒体从状态行后连续铺满 390px 宽，创作信息与固定动作栏之间保留安全距离；390 × 844 下展开入口底部为 745px，动作栏顶部为 767px。
- 颜色与 token：继续使用近黑背景、暖白正文、暖金主动作和绿色成功状态，没有引入新的主题色或卡片体系。
- 图像与图标：结果页使用独立 1122 × 1402 生成图；返回、成功、展开、再次生成与发布图标继续使用本地 Remix Icon。
- 文案与逻辑：明确“已自动存入生成记录”；不再出现“保存作品”；底部主动作统一为“发布 / 投稿”，去向选择仍区分普通来源与活动来源。

### 交互与响应式复核

- [x] 创作信息可展开和收起，`aria-expanded` 与按钮文案同步；长描述可以触发真实页面滚动。
- [x] 普通来源去向顺序为“发布到社区 / 参与活动投稿”，活动来源顺序为“提交到当前活动 / 发布到社区”。
- [x] 图片与视频结果均使用同一结果骨架；视频模式保留 16:9 原生视频、模型、比例和时长信息。
- [x] 360 × 800、390 × 844、430 × 932 三档横向溢出均为 0；固定动作栏贴合视口底部，主动作与再次生成触控高度均为 56px。
- [x] 390 × 844 下主图自然尺寸为 1122 × 1402，实际展示为 390 × 468；凤冠、全身和地面均在首屏构图中完整可见。
- [x] 结果页普通状态不显示走查入口；`?review=1` 仍可打开走查规则面板。
- [x] 浏览器控制台 error = 0；同画面对照未发现剩余 P0/P1/P2 问题。

final result: passed

## 社区、活动与我的移动模块收敛（2026-08-20）

### 对比输入

- 设计事实：用户对当前移动预览提出的明确问题，包括社区去搜索与降低卡片体量、活动卡重做并留在移动路由、我的一级页只保留入口且资料编辑进入独立页面。
- 调整前：`tmp/community-mobile-modules-qa-20260820/community-before.png`、`activity-before.png`、`my-before.png`。
- 调整后：`tmp/community-mobile-modules-qa-20260820/community-final-normalized.png`、`activity-final-normalized.png`、`my-final-normalized.png`；二级页证据包括 `profile-edit-final-390.png` 与 `my-works-final-390.png`。
- 同画面对照：`tmp/community-mobile-modules-qa-20260820/community-compare-top.png`、`activity-compare-top.png`、`my-compare-top.png`；完整页面长度对照为同目录的 `*-compare-full.png`。
- 视口与归一化：浏览器 CSS 视口统一为 390 × 844；调整前原始截图为 750px 宽高密度图，调整后浏览器原始截图按实际移动壳裁切，均归一化为 390px 宽；顶部对照统一裁成 390 × 844 后并排检查。

### 分级发现与修复历史

- P1：社区搜索框占用首屏，单列完整作者头与大媒体卡让每屏只能识别一件内容；已移除搜索框，类型筛选改为轻量下划线，并改成双列紧凑卡，闪念单独横跨双列。
- P1：活动卡最小高度 188px，标题、奖励和动作被纵向拉散；已改为 104px 缩略图与约 112px 高的横向信息卡，保持 6 个当前活动与 2 个往期活动的名称和顺序。
- P1：活动列表有 7 个入口回落 PC 页面；已改为移动通用活动详情、专属生图挑战、任务与邀请页，并逐项检查列表不存在非移动链接。
- P1：“我的”同时承担入口页与作品、投稿、任务、积分和消息详情，且编辑仅触发提示；已收敛成内容、成长与权益、服务三组入口，补齐资料编辑及 8 个移动二级页面。
- P2：活动缩略图初版仍露出封面内被截断的大字；已将进行中活动的裁切焦点移动到 78% 水平位置，优先保留主视觉主体。
- P2：社区初版把闪念放在第二排，最后一张图片单独落左列；已调整 DOM 顺序，前三张图像/视频内容先按两列连续排布，再展示横跨双列的闪念。

### 必要保真面

- 字体与层级：继续使用当前系统字体栈；筛选、卡片标题、辅助信息和二级页任务标题沿用现有字号梯度，没有新增营销式大标题。
- 间距与节奏：社区卡片间距 10px，活动卡片间距 12px，我的分组行高 56px；一级页长度显著缩短，底部导航仍保持 58px 安全区规则。
- 颜色与 token：沿用近黑背景、暖白正文、暖金选中态和绿色进行中状态；未引入新的主题色或后台式面板。
- 图片与资产：作品、活动封面和头像全部复用本地真实资产；图标全部来自本地 Remix Icon，没有手绘 SVG、emoji 或占位图。
- 文案与内容：社区类型、8 个活动名称、个人资产数值和入口名称保持 Web 同对象口径；删除搜索占位与一级页展开内容，没有加入原型解释文案。

### 交互与响应式复核

- [x] 360 × 800、390 × 844、430 × 932 三档共 36 个页面/视口组合，无横向溢出、无 broken image。
- [x] 社区筛选切到“视频生成”后只显示视频卡，选中态和 `aria-pressed` 同步。
- [x] 活动首卡进入 `mobile-activity-detail.html?activity=competition`；通用详情按参数正确显示 Prompt 共创计划并进入移动图片创作页。
- [x] 编辑进入 `mobile-profile-edit.html`，保存后返回 `mobile-my.html`；我的作品、投稿、积分和消息均真实点击进入对应移动二级页。
- [x] 我的内容通过 `#flash` 正确打开闪念分类；活动、我的正文不存在 PC 链接。
- [x] 浏览器控制台 error = 0；移动专项验证 22/22、public-preview 29/29 通过。

### 最终判断

第二轮同画面对照未发现仍需处理的 P0/P1/P2 问题。移动一级页的信息密度、卡片比例与层级归属已经符合本轮明确目标；后续只需在产品走查中继续做 P3 级文案与间距微调。

final result: passed

## 社区移动端图标一致性复核（2026-08-19）

### 对比输入

- 首页：`tmp/community-mobile-icon-audit-20260819/home-before-after.png`。
- 活动：`tmp/community-mobile-icon-audit-20260819/activity-before-after.png`。
- 我的：`tmp/community-mobile-icon-audit-20260819/my-before-after.png`。
- 视口与状态：390 × 844、deviceScaleFactor 1、`?clean=1`；另逐页检查 13 个移动页面的默认首屏状态。

### 复核结果

- [x] 首页 4 个金刚区图标由 18px 调整为 22px，并恢复暖金强调色；卡片结构、文字和跳转不变。
- [x] 五项底部导航图标统一为 22px；首页、社区、活动、我的选中态显示暖金色，中央创作按钮保持金底深色加号。
- [x] 活动卡片入口箭头、个人中心 10 个快捷入口和权益图标均恢复正确对比度；个人中心快捷图标调整为 20px。
- [x] 图片/视频详情、图片/视频生成、生成进度、生成结果、活动详情和投稿详情未发现图标资源缺失、错误变色或固定栏遮挡。
- [x] 13 页共检查 116 个 `.mobile-icon`：`brokenCount = 0`、`unsafeSepiaOnlyCount = 0`、各页 `horizontalOverflow = 0`、控制台 error = 0。

### 问题与修复历史

- P1：强调色规则只使用 `sepia()`，无法把黑色 SVG 提亮，导致金刚区、选中导航、活动箭头和个人中心图标接近黑色；已改为共享金色、酸橙色和绿色语义滤镜。
- P2：首页金刚区 18px 图标在 44px 图标容器中偏小；已调整为 22px，并同步统一一级底部导航尺寸。
- 回归保护：移动端专项验证新增 Remix Icon 来源、禁止 `sepia-only` 滤镜、语义色 token 和底部导航 22px 尺寸检查。

final result: passed

## 社区移动端第一轮 12 个关键视图补齐（2026-08-19）

### 对比输入

- 视觉基线：`tmp/community-mobile-topbar-qa-20260819/source-mobile-create-390x844.png`。
- 实现截图：`tmp/community-mobile-topbar-qa-20260819/implementation-create-image-390x844.png`、`implementation-generation-progress-390x844.png`、`implementation-generation-result-390x844.png`、`implementation-campaign-detail-390x844.png`、`implementation-submission-detail-390x844.png`。
- 同画面对照：`tmp/community-mobile-topbar-qa-20260819/compare-create-source-vs-image-input.png`。
- 视口与密度：源图和实现图均为 390 × 844 像素，对应 390 × 844 CSS 视口、deviceScaleFactor 1；并排图仅增加 16px 分隔，不缩放源图。
- 状态：`?clean=1`；图片输入默认态、图片生成中 68%、图片生成成功未保存、活动进行中、投稿需修改。

### 全视图与重点区域复核

- [x] 13 个移动页面覆盖第一轮 12 个关键视图，活动中心作为额外一级页保留；新增任务页均使用“返回 + 居中任务标题”，不与底部五导航竞争。
- [x] 图片/视频输入、生成中与恢复、结果分流、活动详情、投稿详情沿用现有深色暖金社区的卡片、圆角、间距和图标体系，没有引入第二套移动视觉。
- [x] 图片链路“提示词与参数 → 生成中 → 生成成功 → 保存作品 → 发布/投稿”可操作；视频链路复用同一恢复页并能进入带原生控制的视频结果。
- [x] AI 生图创作挑战保持 Web 的活动名称、封面、长期开放、最高 1000 积分、3 项任务和活动作品；移动端只重排信息密度。
- [x] 投稿详情可从需修改进入编辑，说明为空时阻止重投，成功后变为审核中并新增一条审核记录；原记录不被覆盖。
- [x] 360 × 800、390 × 844、430 × 932 共 39 个页面/视口组合均 `horizontalOverflow = 0`、`brokenImages = []`；控制台 error = 0。

### 必要保真面

- 字体与层级：继续使用当前系统字体栈；任务栏标题、分区标题、字段标签、状态和辅助信息层级清晰，无异常折行。
- 间距与节奏：页面侧边距 16px，表单/状态卡以 12–16px 间距组织；固定动作栏未遮挡最后一项内容，360px 下仍无横向溢出。
- 颜色与 token：沿用 `#05070b` 深色背景、`#f4c95d` 暖金主动作、绿色成功和橙红需修改状态；选中、禁用和错误不只依赖颜色。
- 图像与图标：封面、生成结果和活动图继续复用本地真实资产；操作图标全部来自本地 Remix Icon，修复上传、选择和加载图标在深色背景上的可读性。
- 文案与内容：C端正文只展示业务名称、字段、状态、奖励和动作；原型、配置、研发和审核内部字段仅保留在 `?clean=1` 隐藏的走查面板中。

### 比较历史

- 首轮发现 P2：上传、模型选中和生成加载图标在深色背景上对比不足。
- 修复：为上述外链 Remix Icon 增加暖金滤镜；重新构建并按同一 390 × 844 状态截图。
- 复核：图标与主色一致，新增页面未发现可执行的 P0/P1/P2 问题；后续只进入页面级 P3 微调。

### 边界

- 当前为本地静态产品原型，生成、保存、发布、投稿、审核和积分状态只验证页面路径与状态表达，不代表真实接口或线上能力已经联调。
- 当前页面集合完成不等于移动端适配收口；键盘实机、浏览器后台恢复、弱网、登录续接和真实积分结果仍需后续研发与设备验证。

final result: passed

## 社区移动端 Web 同产品口径与无顶部一级页骨架（2026-08-19）

### 对比输入

- source visual truth path：`tmp/community-mobile-web-parity-qa-20260819/source-web-home-responsive-390x844.png`；同一 Web 首页桌面内容源为 `source-web-home-1280x844.png`，调整前移动视觉基线为 `before-mobile-home-390x844.png`。
- implementation screenshot path：`tmp/community-mobile-web-parity-qa-20260819/after-mobile-home-390x844.png`、`after-mobile-community-390x844.png`、`after-mobile-create-390x844.png`、`after-mobile-activity-390x844.png`、`after-mobile-my-390x844.png`、`after-mobile-image-detail-390x844.png`。
- viewport：浏览器 CSS 视口 390 × 844，deviceScaleFactor 1；浏览器截图原始像素为 375 × 812，首页实现图归一化为 390 × 844 后参与同画面对照。
- state：`?clean=1`、页面顶部；首页 Banner 第 1 张、一级页默认状态、图片详情默认状态。
- full-view comparison evidence：`tmp/community-mobile-web-parity-qa-20260819/compare-web-source-mobile-implementation.png`，左侧为同视口 Web 当前页，右侧为归一化后的移动首页实现。
- focused region comparison evidence：无需额外裁切；同画面对照已能读清 3 个 Banner 的首张、4 个固定入口、“爆款作品”、类型筛选和底部五导航；活动、创作、我的与详情另用同视口实现截图检查首屏密度和固定控制。

### 发现与修复历史

- [P1 已修复] 调整前移动首页自建“新一期生图挑战”、成长待办和“正在被喜欢的作品”，与 Web 当前 3 个 Banner、4 个固定入口和“爆款作品”不一致；现已改为同对象、同标题、同顺序和同跳转目标。
- [P1 已修复] 调整前五个一级页存在品牌、标题、搜索、消息、记录或账户操作组成的顶部应用栏，与底部五导航重复占用首屏；现已全部取消，页面身份分别由 Banner、搜索、正文标题、活动主视觉和资料卡承担。
- [P1 已修复] 调整前活动页只有移动端自建精选活动、筛选和“我的参与”；现已对齐 Web 活动中心的 6 个当前活动与 2 个往期回顾，不保留移动端第二套名称和列表结构。
- [P2 已修复] 详情页分享原位于顶部右侧；现移入底部互动栏，顶部只保留返回和居中“作品详情”。360px 下 4 个次动作与“一键同款”仍完整可达。
- 修复后同画面对照与 6 张关键页截图未发现剩余可执行 P0/P1/P2 问题。

### 必要保真面

- 字体与层级：沿用当前系统字体栈、暖白主文字和区块层级；一级页不再用顶部标题重复页面身份，`AIGC 生成`、`活动中心`、`个人中心`等标题与 Web 当前命名一致。
- 间距与布局：一级页顶部直接进入 16px 安全区正文；首页 Banner、2 × 2 固定入口和双列作品保持 16px 主边线；底部导航与详情动作栏浏览器实测 `bottom = 0`。
- 颜色与 token：继续使用深色社区、暖金主动作、酸橙活动状态和既有细描边，没有引入新配色或后台式白色界面。
- 图像与资产：首页 3 张 Banner、活动封面、作品图、头像和全部图标均复用当前 Web 本地资产与 Remix Icon；浏览器检查 `brokenImages = []`。
- 文案与内容：一级页正文删除原型解释、运营说明和移动端自造营销文案；保留业务标题、内容对象、状态、数据和动作。首页与活动数量、名称及入口由静态合同锁定。

### 验证

- `npm.cmd run validate:community:mobile-h5`：7/7，`failureCount = 0`。
- 浏览器 360 × 800、390 × 844、430 × 932：7 页 `horizontalOverflow = 0`；五个一级页 `appbars = 0`，两个详情页 `appbars = 1`；控制台 error = 0。
- 首页 Banner 点选切换后 active index = 1、scrollLeft = slideWidth；3 个 Banner、4 个固定入口静态与浏览器断言一致。
- `npm.cmd run validate:community:public-preview`：全量 28/29；唯一失败仍为不属于本批的 `competition-my-submissions.html:noBrokenImages`。
- `npm.cmd run validate:community:product-contract`：仍有 1 条既有赛事后台前置校验失败 `competition-contract:b-final-results-require-operational-precheck-before-reward-records`，本轮未跨模块修改。

### 边界

- 这是静态移动端产品原型与本地预览包，不代表账号、生成、保存、发布、审核、积分、兑换、消息或分享能力已与真实接口联调。
- Web 页面在 390px 的当前响应式表现只用于核对内容源，不是移动布局复刻目标；移动端保留同产品事实并按手机重新排版。

final result: passed

## 社区移动端活动/我的导航适配（2026-08-19）

### 来源与实现证据

- 移动壳视觉来源：`tmp/community-mobile-nav-audit-20260819/01-mobile-home-source.png`。
- 问题证据：`tmp/community-mobile-nav-audit-20260819/02-activity-pc-destination.png`、`03-my-pc-destination.png`。
- 修复后截图：`tmp/community-mobile-nav-audit-20260819/06-activity-mobile-final.png`、`05-my-mobile-fixed.png`。
- 视口：390 × 844，deviceScaleFactor 1；另以 360 × 800、430 × 932 检查响应式布局。
- 状态：`?clean=1`；活动默认“全部”，我的默认资料与成长概览。

### 复核结果

- [x] 五项底部导航统一指向 `mobile-home.html`、`mobile-community.html`、`mobile-create.html`、`mobile-activity.html`、`mobile-my.html`。
- [x] 活动页包含活动筛选、参与状态、继续创作和我的参与；“已结束”筛选只显示 1 个对应活动。
- [x] 我的页包含用户信息、作品、投稿、任务、积分、兑换、邀请、消息和收藏入口；签到后积分从 1280 更新为 1300。
- [x] 360/390/430 三档视口均 `horizontalOverflow = 0`，底部导航固定在视口底部，新增页无 broken image，控制台 error = 0。
- [x] 字体、间距、暖金/酸橙 token、本地 Remix Icon 和现有活动/用户资产保持移动首页基线。

### 比较历史

- P1：活动和我的一级入口跳回桌面页，破坏移动端连续体验；已新增两个移动一级页并统一所有移动底部导航目标。
- P2：活动列表首轮使用含大字海报作为窄卡封面，裁切后出现残缺文字；已替换为无字横幅资产并调整焦点位置。
- 修复后功能与分视图视觉检查未发现剩余 P0/P1/P2 问题。

### 当前阻塞

- 浏览器安全策略不允许生成用于严格设计 QA 的本地同画面对照输入；现有来源和实现截图已分别检查，但不满足 Product Design `design-qa` 的同一比较输入要求。

final result: blocked

## 社区移动端 H5 第一批 5 视图（2026-08-18）

### 对比输入

- 视觉基线：当前深色社区 C端原型；移动视口现状截图为 `tmp/community-mobile-h5-qa-20260818/source-current-mobile.png`。
- 实现截图：`tmp/community-mobile-h5-qa-20260818/implementation-mobile-home.png`、`implementation-mobile-community.png`、`implementation-mobile-image-detail.png`、`implementation-mobile-video-detail.png`、`implementation-mobile-create-video.png`。
- 浏览器视口：390 × 844，deviceScaleFactor 1；另以 360 × 800 和 430 × 932 完成布局矩阵复核。
- 比较状态：均使用 `?clean=1` 隐藏内部走查面板；首页首屏、社区默认流、图片/视频详情首屏和视频创作选中态。
- 说明：本次是移动端信息架构改造，不要求与桌面压缩态逐像素一致；保留深色背景、暖金主动作、现有品牌和本地作品资产，重排为移动顶部栏、内容卡片和固定底部导航/详情动作栏。

### 必要界面检查

- [x] 移动首页保留活动主入口、创作、新手任务、推荐作品和五项底部导航，首屏主动作明确。
- [x] 社区页支持图片、视频、闪念筛选和搜索空状态；卡片操作不挤占主要内容阅读。
- [x] 图片与视频详情不保留一级底部导航，固定详情动作栏承接点赞、评论、收藏和一键同款。
- [x] 创作首页将图片生成和视频生成并列为 P0，URL 参数可选中对应模式，并保留“新一期生图挑战”上下文。
- [x] 360、390、430 三档视口的页面、顶部栏、底部导航和详情动作栏均无横向溢出，固定栏贴合视口底部。
- [x] 首页 → 社区 → 图片详情 → 一键同款 → 图片创作模式路径通过；社区筛选、搜索空状态、点赞和评论交互通过；浏览器控制台 error = 0。

### 分级问题与修复历史

- P0：无。
- P1：无。
- P2：首轮视频卡片和视频详情文案与真实灯塔素材不一致；已统一为“海雾灯塔 · 情绪片头”，并同步作者、提示词和评论语义。
- P3：改造前移动视口会直接压缩桌面导航且首屏被新手任务弹层占据；第一批实现将登录/任务引导收回业务路径，改为移动顶部栏与五项底部导航。这是已确认的结构性改造，不作为视觉偏差。

### 边界

- 当前为本地 HTML 产品原型；该批次交付时活动和我的曾复用桌面页，已在 2026-08-19 的“社区移动端活动/我的导航适配”中补齐移动专属视图。
- 静态交互和状态用于验证产品路径，不代表真实账号、生成、保存、发布、审核、积分或消息已经联调。
- 全量产品合同仍有一条既有赛事后台前置校验未通过；全量公开预览另有既有“我的投稿”页 3 张赛事图片被判为 broken。两项均不属于本批移动端页面和共享资源，本轮未跨模块修改。

final result: passed

## 赛事首屏参考图表现形式适配（2026-08-13）

### 对比输入

- 参考图：`C:/Users/hongx/AppData/Local/Temp/codex-clipboard-a7d54079-ac96-4414-b716-27cd75691cda.png`
- 实现截图：`outputs/competition-design-qa/competition-hero-implementation.png`
- 移动端截图：`outputs/competition-design-qa/competition-hero-mobile.png`
- 并排对照：`outputs/competition-design-qa/competition-hero-reference-comparison.png`
- 参考图像素：1350 × 446；实现截图像素与 CSS 视口：1280 × 720，deviceScaleFactor 1；移动端：390 × 844，deviceScaleFactor 1。
- 状态：赛事进行中、作品征集、用户已报名、投稿可继续编辑。

### 对比结论

- [x] 信息架构沿用参考图的“居中赛事身份 + 阶段胶囊 + 底部赛事详情台”，没有照搬参考品牌、奖池数字或参与人数。
- [x] 字体与层级：主标题保持当前赛事展示字体和暖金强调；标题、口号、状态、详情信息形成四级层次，桌面与移动端均无异常折行。
- [x] 间距与布局：桌面详情台采用一块主题信息和两块状态卡；移动端改为纵向堆叠，并完整落入 390 × 844 首屏。
- [x] 颜色与视觉：保留多元拾光深色社区、暖金主色和电影感视频；半透明详情台提高读取稳定性，没有退化成后台仪表盘。
- [x] 图像与图标：继续使用本地赛事视频和 Remix Icon 资源，图标无缺失，视频主体未被详情台遮挡。
- [x] 文案与事实：只使用已确认的活动时间、四条赛道、成人个人参赛和动态参赛状态；未虚构总奖池、参与人数或地点。
- [x] 主操作：已报名且存在投稿版本时，“继续编辑”可进入对应投稿页；浏览赛事作品入口保留；控制台错误为 0。
- [x] 桌面与移动端 `horizontalOverflow = 0`，所有图片加载成功。

### 设计差异边界

- 参考图使用静态点阵背景，本实现保留已确认的一屏赛事视频，这是有意保留的品牌与内容资产差异。
- 参考图底部展示奖池、参与者和截止日期；当前产品没有可靠奖池与参与人数口径，因此改为赛事主题、活动时间和我的参赛操作。
- 参考图没有社区导航，本实现保留现有三项赛事导航和用户身份入口，避免破坏已确认页面骨架。

### 比较历史

- 首轮：浏览器命中旧 CSS 版本缓存，结构已更新但样式未加载，且新增图标未进入预览包。
- 修复：提升赛事 CSS 版本号，并切换到预览包已有的本地 Remix Icon。
- 复核：桌面 1280 × 720、移动端 390 × 844 均无破图和溢出，首屏结构与参考图目标一致。

final result: passed

## 社区统一作品详情弹层复核（2026-08-13）

### 对比输入

- 社区首页详情弹层基线：`tmp/competition-work-modal-20260813/01-community-reference.png`
- 赛事作品详情首版：`tmp/competition-work-modal-20260813/02-competition-modal-desktop.png`
- 桌面端收敛结果：`tmp/competition-work-modal-20260813/03-competition-modal-desktop-refined.png`
- 移动端修复结果：`tmp/competition-work-modal-20260813/06-competition-modal-mobile-final.png`

### 可见结果

- [x] 参赛作品卡普通点击在列表原页打开详情弹层，不打断筛选与浏览上下文。
- [x] 赛事头部只保留活动名称，不重复展示活动描述；“我的作品”视图直接进入作品列表，不再插入个人阶段结果摘要。
- [x] 弹层复用社区内容详情的左侧媒体、右侧作者/标题/标签/互动/信息与评论结构；赛事只追加紧凑参赛关系、投票和本人状态。
- [x] 点赞、收藏、分享和评论属于社区作品互动；赛道和个人阶段结果属于作品与赛事关系，热度只用于发现排序，不参与最终成绩。
- [x] 作品卡保留真实 `competition-work.html?submission_id=...` 链接；新标签、分享、刷新与外部分发可进入同结构独立页。
- [x] 弹层与独立页使用同一 `submission_id` 和同一渲染器，标题、作者、媒体与公开字段一致。
- [x] 关闭按钮、遮罩、Escape、浏览器前进后退和触发卡焦点恢复形成完整闭环。
- [x] 桌面端 1210×884 双栏结构与社区基线一致，媒体为第一视觉层，互动动作在首屏可见。
- [x] 移动端 390×844 为全屏详情，媒体宽度 390px、正文单独滚动、关闭入口可见，`horizontalOverflow=0`。

### 边界

- 本轮只统一详情形态、作品身份与深链合同；尚未把赛事作品实际分发到社区首页或 AIGC 内容流。
- 未来分发必须继续引用同一公开作品/`submission_id`，不得复制出第二套点赞、收藏、评论或分享数据；赛事票数仍保持独立。

## B/C 端参赛规则生命周期重排（2026-08-17）

### 对比输入

- 用户选定概念图：`C:/Users/hongx/.codex/generated_images/01a00aef-b4ee-7c80-9685-f4ee3a7887cf/exec-a96b0c32-a8ee-4a5b-9e31-de38561420ed.png`
- B 端实现截图：`outputs/competition-rules-design-qa/b-rules-top.png`
- C 端实现截图：`outputs/competition-rules-design-qa/c-rules.png`
- B/C 合并截图：`outputs/competition-rules-design-qa/implementation-combined.png`
- 同画面对照：`outputs/competition-rules-design-qa/reference-vs-implementation.png`
- 参考图像素：1448 × 1086；B/C 浏览器截图：1240 × 884，deviceScaleFactor 1；对照图：1540 × 812。
- 验收状态：B 端已发布赛事、7/7 章节就绪；C 端读取同一赛事已发布规则。

### 全局与重点区域复核

- [x] B 端使用独立“参赛规则”页签，以报名前、投稿前、评选中、结果后、争议处理 5 个生命周期阶段编排 7 个固定章节；没有新增、隐藏、删除或排序入口。
- [x] B 端右侧只展示章节唯一事实来源、C 端摘要、配置状态和维护入口；赛事特殊说明继续通过 Ant Design 弹层集中编辑。
- [x] C 端保留左侧 5 阶段导航与正文阶段标题；顶部阶段路径和右侧后续阶段已按走查反馈移除。正文仍保留 7 个固定章节 ID，锚点和已发布补充说明链路不变。
- [x] 字体、颜色与密度分别遵守既有 B 端 Ant Design 白色后台和 C 端深色暖金社区基线，没有把两端强行做成同一种视觉皮肤。
- [x] B 端选中层级、章节状态点和维护入口清晰；C 端标题、阶段、章节正文和辅助导航层级可辨，没有出现过密控制或悬浮遮挡。
- [x] B/C 桌面视口 `horizontalOverflow = 0`；B 端章节切换和编辑弹层关闭有效，C 端阶段锚点和赛事助手弹层有效。

### 分级问题与修复历史

- P0：无。
- P1：无。
- P2：首轮静态校验仍按“面板仅有 `panel` 单类名”和 9 个页签提取规则，无法识别新增规则页签；已更新为兼容组合类名，并将赛事工作台合同同步为 10 个页签。
- P3：概念图在 B 端右侧直接放置补充说明输入框；实现遵循现有后台“摘要只读、编辑进标准弹层”的已确认交互规范，这是有意差异。
- 复核：B 端静态、交互与 31 页公开预览验证通过；C 端规则页专项公开预览验证通过，赛事规则专项合同与固定章节 C/B 投影检查通过。

### 边界

- 当前为本地 HTML 产品原型，浏览器存储仅模拟同赛事草稿/已发布规则联动，不代表真实接口或线上发布完成。
- 全量赛事、产品合同和 C/B 覆盖仍受工作区既有 29 页/28 页登记差异及其他赛事模块未收口项影响；本次规则重排相关检查已单独通过。

final result: passed

## C 端规则页冗余导航移除（2026-08-17）

### 对比输入

- 调整前浏览器截图：`outputs/competition-rules-design-qa/c-rules.png`
- 调整后浏览器截图：`outputs/competition-rules-design-qa/c-rules-after-removal-top.png`
- 同画面对照：`outputs/competition-rules-design-qa/c-rules-removal-comparison.png`
- 浏览器视口与截图像素：1240 × 884，deviceScaleFactor 1；状态为已发布赛事规则首屏。

### 复核结果

- [x] 顶部“参赛规则路径”已移除，赛事标题下方直接进入规则导航与正文。
- [x] 右侧“即将进行 / 后续阶段”已整体移除，不保留空容器或空白列。
- [x] 桌面布局收敛为 190px 左侧阶段导航 + 970px 正文区域，正文完整利用释放出的宽度。
- [x] 左侧 5 阶段导航、7 个章节锚点、滚动高亮和赛事助手弹层继续可用。
- [x] 字体、暖金色 token、正文层级和既有图片资产未变化；本次没有新增或替换视觉资产。
- [x] `horizontalOverflow = 0`，浏览器 DOM 中 `.competition-rule-journey` 和 `.competition-rule-next` 均不存在。

### 比较历史

- P0/P1：无。
- P2：移除右侧栏后若沿用三列网格会留下无意义空列；已同步改为两列网格并将正文扩展到剩余空间。
- 复核证据：`c-rules-removal-comparison.png`；调整后未发现可执行的 P0/P1/P2 问题。

### 边界

- B 端“参赛规则编排”不变；本次只精简 C 端阅读页的重复导航。
- 当前仍是本地 HTML 原型，未提交、推送或发布。

final result: passed

## 社区移动端弱顶部应用栏收敛（2026-08-19）

### 对比输入

- 视觉基线：`tmp/community-mobile-topbar-qa-20260819/before-mobile-home-390x844.png`、`before-mobile-community-390x844.png`、`before-mobile-my-390x844.png`、`before-mobile-image-detail-390x844.png`。
- 实现截图：`tmp/community-mobile-topbar-qa-20260819/after-mobile-home-390x844.png`、`after-mobile-community-390x844.png`、`after-mobile-my-390x844.png`、`after-mobile-image-detail-390x844.png`。
- 同画面对照：`tmp/community-mobile-topbar-qa-20260819/compare-home-before-after.png`、`compare-community-header-before-after.png`、`compare-my-header-before-after.png`、`compare-detail-header-before-after.png`。
- 视口与密度：源图和实现图均为 390 × 844 像素，对应 390 × 844 CSS 视口、deviceScaleFactor 1；同画面对照为等尺寸图像水平拼接，不做缩放。
- 状态：`?clean=1`、页面顶部；另检查社区滚动态、搜索聚焦态和 360 × 800、430 × 932 响应式状态。

### 全视图与重点区域复核

- [x] 首页全视图只移除品牌副标并减弱静止态栏背景；活动 Hero、快捷入口、推荐作品和底部五导航的位置与密度保持不变。
- [x] 社区重点区域将消息替换为更贴合当前页的搜索快捷入口；点击后焦点准确落到 `#mobile-community-search`，头像入口保留。
- [x] 我的页不再重复显示“我的”标题，资料卡继续承担页面身份；消息和设置仍固定在右侧，正文未上移遮挡安全区。
- [x] 图片/视频详情使用对称三列布局，返回、居中类型和分享分别占据稳定位置；一级底部导航继续隐藏，详情动作栏不变。
- [x] 滚动超过 8px 后顶部栏进入 `is-scrolled`，稳定背景为 `rgba(5, 7, 11, 0.9)`、分隔线为 `rgba(255, 247, 230, 0.08)`，保持粘性且不遮挡首字段或媒体。

### 必要保真面

- 字体与层级：继续使用当前系统字体栈；一级页标题维持 17px/800，首页品牌收敛为 15px 单行，详情标题保持居中且无异常折行。
- 间距与节奏：应用栏内容高度 48px，浏览器实测含分隔线为 49px；右侧动作不超过 2 个，触控区保持 44 × 44px，正文和底部导航节奏未变化。
- 颜色与 token：沿用深色社区、暖金积分与选中态；静止态只降低顶部容器感，滚动态恢复深色半透明背景、模糊和轻阴影。
- 图像与图标：作品、头像、品牌资产均未替换；搜索、消息、设置、返回、分享继续使用本地 Remix Icon，无新增占位或手绘图标。
- 文案与内容：首页删除重复解释性的品牌副标，我的页删除与底部导航重复的标题；其余用户文案与业务事实保持不变。

### 比较历史与验证

- 首轮同画面对照未发现可执行的 P0/P1/P2 问题，因此未进入视觉修复循环；剩余差异均是本轮已确认的顶部应用栏裁决。
- 7 页专项静态验证通过；360/390/430 三档均 `horizontalOverflow = 0`、顶部栏 `top = 0`、底部导航或详情动作栏贴合视口底部。
- 社区搜索聚焦、顶部栏滚动状态和详情居中布局通过；浏览器控制台 error = 0。
- public-preview 29/29 通过；全量产品合同仍有一条既有赛事后台前置校验失败，与本轮移动顶部栏无关，本轮未跨模块修改。

final result: passed

## 社区移动首页内容流轻量化（2026-08-19）

### 对比输入

- 调整前：`tmp/community-mobile-home-feed-qa-20260819/before-home-390x844.png`。
- 调整后：`tmp/community-mobile-home-feed-qa-20260819/after-home-top-390x844.png`；视频筛选态为 `after-home-video-filter-390x844.png`。
- 全视图对照：`tmp/community-mobile-home-feed-qa-20260819/home-before-after-390x844.png`。
- 重点区域对照：`tmp/community-mobile-home-feed-qa-20260819/home-feed-before-after-focus.png`。
- 视口与密度：源图和实现图均为 390 × 844 像素，对应 390 × 844 CSS 视口、deviceScaleFactor 1；状态均为页面顶部、`?clean=1`。

### 分级发现与修复历史

- P1：首页固定入口会把用户带到尚未完成移动适配的页面；已移除整组入口，不保留空白占位。
- P1：爆款作品分类使用链接，点击图片、视频、文本或短剧漫剧会进入桌面页面；已改为本页按钮筛选，5 个分类切换时 URL 保持不变。
- P2：胶囊标签在窄屏上占据过多视觉重量；已改为透明文字标签与 2px 暖金选中下划线，触控高度保持 44px。
- P2：两列网格按行拉齐卡片高度，横图与竖图产生大块空白；已改为双列 CSS 瀑布流，媒体按原始宽高比展示，卡片高度由内容决定。

### 必要保真面

- 字体与层级：保留系统字体栈和“爆款作品”标题层级；分类使用 12px/750，不新增解释性文案。
- 间距与节奏：Banner 后直接进入内容区；分类与瀑布流使用 13px/10px 节奏，底部导航不遮挡最后一列内容。
- 颜色与 token：继续使用深色社区、暖白正文和暖金选中态；筛选不再使用大面积金色填充。
- 图像与资产：继续使用现有真实作品图片，增加原始宽高属性并取消强制裁切；未新增占位、手绘图标或外部图片。
- 文案与内容：保留全部、图片生成、视频生成、文本内容、短剧漫剧 5 个既有分类名称；固定入口相关文案随模块一起移除。

### 交互与响应式复核

- [x] 5 个分类逐一点击后，显示数量依次为全部 6、图片 2、视频 1、文本 2、短剧漫剧 1；每次切换 URL 均不变，`aria-pressed` 只有一个选中项。
- [x] 360 × 800、390 × 844、430 × 932 三档均 `horizontalOverflow = 0`，无 broken image，瀑布流存在至少两种卡片高度。
- [x] 首页没有 `data-mobile-home-entry`；作品详情链接继续进入移动端图片或视频详情页；控制台 error = 0。
- [x] 全视图与重点区域对照未发现剩余 P0/P1/P2 问题；移动端专项验证通过。

final result: passed

## 社区移动首页瀑布流行优先修正（2026-08-19）

### 对比输入

- 视觉问题依据：浏览器批注“优先占满第一行，再进行第二行布局”，对应图片生成筛选态下两张作品连续堆叠在左列。
- 调整前：`tmp/community-mobile-home-row-first-qa-20260819/before-image-filter-390x844.png`。
- 调整后：`tmp/community-mobile-home-row-first-qa-20260819/after-image-filter-row-first.png`；390 × 844 移动视口证据为 `after-image-filter-row-first-390x844.png`。
- 重点区域对照：`tmp/community-mobile-home-row-first-qa-20260819/row-first-before-after-focus.png`。
- 对照图使用相同 1303 × 884 浏览器视口、相同图片生成筛选态和相同移动页面外壳裁切；移动端另以 390 × 844 CSS 视口、deviceScaleFactor 1 复核。

### 分级发现与修复

- P1：原 CSS 多栏布局按“先填满一列、再进入下一列”分配内容；筛选结果只有两张作品时，两张卡片会全部落在左列，右列为空。
- 已改为两列 CSS Grid，并根据每张可见卡片的实际高度计算行跨度；卡片按 DOM 顺序先占满首行，后续卡片再向当前较短列补位。
- 分类切换、图片加载、字体就绪和视口变化时会重新测量；隐藏卡片不参与布局计算。

### 必要保真面

- 字体、颜色、图片、标题和筛选文案未改；Banner、底部导航和卡片内容保持原样。
- 图片继续按原始宽高比展示，两列卡片不强制等高。
- 只修正作品容器的排布规则，筛选仍在当前页完成且 URL 不变。

### 交互与响应式复核

- [x] 390 × 844 下，图片生成与文本内容两个双卡分类均在同一纵坐标开始，分别占据左右两列。
- [x] 全部分类前两张可见作品先占满首行，后续作品向较短列补位；图片生成两张卡片高度分别约 238px、374px，仍保持自适应高度。
- [x] 视频生成与短剧漫剧的单卡状态均从左列开始，无异常空列或位移。
- [x] 5 个分类逐一切换后 URL 均不变，`horizontalOverflow = 0`，控制台 error = 0。
- [x] 移动端专项验证 13/13 通过。

final result: passed

## 社区移动 Banner 与作品详情层级修正（2026-08-19）

### 对比输入

- 视觉问题依据：用户对当前移动详情页提出四项明确反馈，包括 Banner 不得回落桌面页、图片铺满宽度、标题话题前置和评论层上移。
- 调整前：`tmp/community-mobile-detail-qa-20260819/before-detail-390x844.png`、`before-comments-390x844.png`。
- 调整后：`tmp/community-mobile-detail-qa-20260819/after-detail-390x844.png`、`after-comments-390x844.png`。
- 同画面对照：`tmp/community-mobile-detail-qa-20260819/detail-before-after-390x844.png`、`comments-before-after-390x844.png`。
- 源图与实现图均为 390 × 844 像素，对应 390 × 844 CSS 视口、deviceScaleFactor 1；详情对照均为页面顶部，评论对照均为“全部评论”打开态。

### 分级发现与修复历史

- P1：首页 3 个 Banner 分别链接桌面 AIGC、桌面活动中心和桌面模型广场，移动用户会离开移动骨架；已改为移动视频创作、移动生图挑战详情和移动视频创作，并逐个真实点击复核。
- P2：图片详情使用 `contain`，竖图在固定高度容器内出现左右空边；已改为 3:4 媒体容器内 `cover`，铺满可用宽度并保留凤冠和人物主体。
- P2：作者信息先于作品标题和话题，内容判断层级倒置；图片、视频详情已统一为“媒体 → 标题与话题 → 作者 → 创作信息”。
- P2：全部评论层高度由内容决定，评论较少时层级偏低且输入区不稳定；已将评论层固定为 `min(40dvh, 360px)`，评论列表独立滚动并让输入栏贴底。

### 必要保真面

- 字体与层级：标题、作者、辅助信息字号和字重未改，只调整标题话题与作者的先后关系。
- 间距与节奏：标题与作者间距固定为 16px；详情正文继续使用 16px 水平安全边距，媒体保持全宽。
- 颜色与 token：继续使用当前近黑背景、暖白正文、暖金标签和主动作，不引入新色值。
- 图像与资产：复用原作品图；cover 后人物和凤冠清晰，未拉伸、未替换资产，允许按 3:4 容器裁切上下冗余区域。
- 文案与内容：Banner、作品标题、话题、作者、创作信息和评论原文均保持不变；页面未新增解释性用户文案。

### 交互与响应式复核

- [x] 3 个 Banner 依次进入 `mobile-create-video.html`、`mobile-campaign-detail.html`、`mobile-create-video.html`，均未跳转桌面页面。
- [x] 390 × 844 下图片媒体为 375 × 500，`object-fit = cover`；标题 y=566、作者 y=646，层级顺序正确。
- [x] 评论层为 338px 高、顶部 y=506，覆盖正文和作者区域，同时保留约 457px 主图可见；输入栏底边与视口底边一致。
- [x] 360 × 800、390 × 844、430 × 932 三档 `horizontalOverflow = 0`；评论层高度分别为 320、338、360px。
- [x] 图片与视频详情均完成标题/作者顺序和评论层复核；评论发送后新评论置顶、输入清空，控制台 error = 0。
- [x] 移动端专项验证 13/13、public-preview 29/29 通过；全量产品合同仍有一条既有赛事后台前置校验失败，与本轮移动页面无关。

final result: passed

## 移动邀请页“拾光邀请函”第三版实现（2026-08-20）

### 对比输入

- 选定视觉源：`C:/Users/hongx/.codex/generated_images/01a01d3e-53d8-7160-bddd-5bcbdcb3c605/exec-20f49ea7-fa60-4459-8a78-f83dbb0b7ed0.png`，852 × 1842 像素。
- 首轮实现：`tmp/mobile-invite-design-qa-20260820/implementation-390x844-v1.png`；首轮同屏对照：`tmp/mobile-invite-design-qa-20260820/comparison-v1.png`。
- 最终实现：`tmp/mobile-invite-design-qa-20260820/implementation-390x844-v2.png`；最终同屏对照：`tmp/mobile-invite-design-qa-20260820/comparison-v2.png`。
- 视觉源以 Lanczos 等比归一为 390 × 844，与 390 × 844 CSS 视口、deviceScaleFactor 1 的实现截图并排比较；状态为页面顶部、`?clean=1`、邀请规则层关闭。
- 邀请函主视觉资产：`outputs/community-homepage-style-exploration/assets/mobile/invite-pass-background.webp`，800 × 1135；按选定稿生成无文字的黑金邀请函底图并压缩为约 93 KB，页面文案、邀请码和复制动作继续使用可访问的 HTML 控件。

### 全视图与重点区域证据

- 全视图对照确认顶部标题、两行主标题、邀请函、累计奖励、三步流程、最近进度和底部双动作的纵向层级一致。
- 重点区域复核邀请函与底部行动区：邀请函保持单一视觉焦点，邀请码可复制；最终最近进度区域为 y=618—731，固定行动区从 y=744 开始，与参考图的首屏节奏对齐。
- 邀请函图案、邀请码、好友状态和底部按钮在 390px 原生尺寸下均可清晰判断，因此不再单独裁切更小区域。

### 分级发现与修复历史

- 首轮 P2：实现使用偏重的无衬线主标题，和参考图的宋体编辑感不一致；已切换到系统宋体优先栈，并降低字重。
- 首轮 P2：最近进度卡高度约 126px，固定行动区从 y=770 开始，导致“查看邀请规则”落到首屏之外；已压缩进度行高和区间间距，将规则入口收进底部行动区，最终行动区顶部为 y=744。
- 首轮 P2：邀请函与主标题之间略松；已将邀请函整体上移并统一 7px 段落节奏，奖励、流程和最近进度随之上移。
- 修复后同屏对照未发现剩余 P0/P1/P2；好友印章的锯齿装饰比概念图克制，归类为不影响结构与任务的 P3 品牌细节。

### 必要保真面

- 字体与层级：主标题使用宋体优先栈，页面标题和 UI 文案继续沿用现有系统无衬线；奖励数字使用衬线字体形成对比。
- 间距与节奏：390px 下保持 16px 页面安全边距；最近进度 113px 高，底部行动区在 744px 起固定展示，不遮挡核心内容。
- 颜色与 token：保持近黑背景、香槟金主色、暖白正文和绿色成功状态，没有引入后台仪表盘配色。
- 图像与资产：邀请函使用独立生成的本地位图，品牌标记和所有操作图标使用现有品牌 SVG 与本地 Remix Icon；无外部图片依赖、无破图、无手绘 SVG。
- 文案与内容：明确“注册 + 首次创作”资格、双方 30 积分、累计 90 积分、下一位奖励、两条好友进度；邀请码继续使用原型虚构值 `SG20A666`。

### 交互、响应式与验证

- [x] 复制邀请码后提示“邀请码已复制”；用户邀请规则层可打开、关闭；顶部“明细”定位 `#mobile-invite-progress`。
- [x] “发送邀请函 / 更多分享”共用系统分享和链接复制兜底；静态原型只验证入口与事件，不代表真实邀请关系或积分已联调。
- [x] 360×800、390×844、430×932 三档 `horizontalOverflow = 0`，固定操作区贴底，broken images = 0。
- [x] 移动端专项 24/24、产品合同、C/B 覆盖和正式 public-preview 29/29 通过；全量预览检查失败页和顶层失败项均为 0。

### 开放问题与后续细化

- 真实分享卡片缩略图、渠道文案和邀请落地页参数仍需在真实 C端研发合同中确认；当前不把系统分享结果或积分到账描述为已联调能力。
- P3：如设计后续需要更接近概念稿，可单独优化好友印章边缘纹理，不影响本轮交付。

final result: passed
